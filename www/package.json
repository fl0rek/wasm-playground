{
  "private": true,
  "scripts": {
    "build:wasm": "wasm-pack build --target web --out-dir ../www/pkg ../wasm-app",
    "build": "npm run build:wasm && webpack --config webpack.config.js",
    "start": "webpack-dev-server"
  },
  "devDependencies": {
    "@webpack-cli/serve": "*",
    "copy-webpack-plugin": "*",
    "webpack": "*",
    "webpack-cli": "*",
    "webpack-dev-server": "*"
  }
}
